<%= form_for(@account,:url=>merchant_accounts_path) do |f| %>
  <h3>Create New Account</h3>
  <h4>Company Information</h4>
  
  <%= f.text_field :registered_company_name,:placeholder=>"Company Name" %><br>
  <%= f.text_field :address,:placeholder=>"Address"%>
  <%= f.text_field :pincode,:placeholder=>"Pincode"%>
  <%= f.select :city,options_for_select(%w[Mumbai Pune]),:placeholder=>"City"%>
  <%= f.select :area_id,options_for_select(%w[East West]),:placeholder=>"Area"%><br>
  <%= f.select :currency_id,options_for_select(%w[Rupees Euro]),:placeholder=>"Currency"%>
  <br><br>
  <%= f.fields_for :users do |builder| %>
    <%= render "user_fields", :f => builder %>
  <% end %>
  <br><br>
  <% if f.object.new_record? %>
    <%= f.submit "Register",:class=>"btn btn-primary"%>
  <% else %>
    <%= f.submit "Update",:class=>"btn btn-primary"%>
  <% end %>
<% end %>
