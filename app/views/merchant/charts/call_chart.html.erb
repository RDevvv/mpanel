<script type="text/javascript" charset="utf-8">
    $(function () {
        new Highcharts.Chart({
            chart: { renderTo: 'call_chart', type: 'column' },
            title: { text: 'call by day' },
            xAxis: { type: 'datetime' },
            yAxis: {
                title: { text: 'Call'}
            },
            tooltip: {
                formatter: function () {
                    return Highcharts.dateFormat("%B %e %Y", this.x) + ': ' +
                            'SMS-' + Highcharts.numberFormat(this.y, 0);
                }
            },
            series: [{
                pointInterval: <%= 1.day * 1000 %>,
                pointStart: <%= 30.days.ago.at_midnight.to_i * 1000 %>,
                data: <%= @button_click %>
            }]
        });
    });
</script>

<div id="call_chart" style="width: 800px; height: 700px;"></div>