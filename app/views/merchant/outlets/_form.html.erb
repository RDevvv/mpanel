<%= render :partial=>'header' %>
<div class="row-fluid">
  <div class="span12">
    <div class="box">

      <div class="box-header">
        <span class="title"><i class="icon-account"></i> Outlet </span>
      </div>
      <div class="box-content">
        <%= form_for([@current_account,@account_brand,@outlet], :url=>merchant_account_account_brand_outlets_path(@current_account,@account_brand,@outlet), :html=>{:class=>"form-horizontal fill-up validatable",:multipart => true}) do |f| %>
          <div class="padded">
            
            <%= render :partial=>"outlet_field",:locals=>{:f=>f} %>
            
          </div>  
          <div class="form-actions">
						<button type="submit" class="btn btn-blue">Save</button>
          </div>
        <% end %>
      </div>  
    </div>
  </div>
</div>      
