        <% @map_outlets.push([poster[:latitude],poster[:longitude],poster[:outlet_id]]) %>
        <li class="mix bsc span12" data-outlet="<%= poster[:outlet_id] %>">
        <div class="bdiv2" outlet_id="<%= poster[:outlet_id] %>" ad_id="<%= poster[:ad_id] %>"id="<%= poster[:outlet_id] %>">
        <div class="bbox2">
            <div class="brandtitle">
                <%= get_brand_title(poster[:brand_name]) %>
            </div>
                    <div class="rch-btns-div4">
                          <div class="btn190"> <a class="button_view">View</a>
                        </div>
                        <div class="btn190"><a class="ad_request button_offer">Get Offer</a>
                        </div>
                    </div>
                <% unless poster[:ad_is_exclusive].blank? %>
            <div class="eye2"></div>
            <% end %>
            <div class="distance-div">Dist: <%= poster[:distance].to_f.round(2) %> km</div>
            <div align="center" class="brandlogo">
                <%= get_brand_image(poster[:brand]) %>
            </div>
            <div class="distance-div-below">Ends: <%= poster[:ad_expiry_date].strftime('%d/%m') %></div>
            <div class="lower-box2">
                <a class="share" >
                    <div class="refr2"></div>
                </a>
                <p>Availed: <%= poster[:ad_usage] %></p>
            </div>
        </div>
    </div>
    <div class="pin-add-div">
        <div class="pin-div">
            <div class="pin-box"><%= (@pin_id+65).chr %></div>
            <% @pin_id += 1 %>
        </div>
        <div class="add-box">
            <div class="adtitle1">Address</div>
            <div class="adaddress"><%= "#{poster[:shop_no]} #{poster[:address]}" %></div>
            <div class="adloc"><%= poster[:area_name] %><br/>
                <%= poster[:city_name] %> <%= poster[:pincode] %></div>
                <% unless poster[:mobile_number].blank? and poster[:phone_number].blank? %>
                    <% if check_if_verified(cookies[:customer_uuid])==1 %>
                        <a class="call button_call3" href="tel:09223584822">CALL</a>
                    <% else %>
                        <a class="call button_call3" onclick="call_button_verification()">CALL</a>
                    <% end %>
                <% end %>
            </div>
        </div>
    </li>
