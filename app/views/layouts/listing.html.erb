<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0"/>
<title>Gullak Master - <%= generate_title %></title>
<%= csrf_meta_tags %>

<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->

<%= stylesheet_link_tag "application", :media => "all" %>
<%= javascript_include_tag "application" %>

<!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
<!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
<!-- jQuery Plugin -->
<%#= javascript_include_tag :defaults %>


</head>
<body>
<!-- Preloader -->
<div id="header">
  <!--<div class="map-btn-div"><a href="#" class="map-btn-lft2">&nbsp;</a></div>-->
  <ul class="left-icons">
    <li><a href="#menu" class="srchlink3"></a></li>
  </ul>
  <div class="head-brand">GullakMaster</div>
  <ul class="right-icons">
  <% if action_name == 'map_listing' %>
      <li><%= link_to "", {:controller => 'home', :action => 'outlet_listing',:location => params[:location],  :longitude => params[:longitude], :latitude => params[:latitude]}, :class => "outlet"%></li>
  <% elsif action_name == 'outlet_listing' %>
      <li><%= link_to "", {:controller => 'home', :action => 'map_listing', :location => params[:location], :longitude => params[:longitude], :latitude => params[:latitude]}, :class => "loc"%></li>
  <% elsif action_name == 'map_search' %>
      <li><%= link_to "", {:controller => 'home', :action => 'outlet_search',:location => params[:location],  :longitude => params[:longitude], :latitude => params[:latitude], :search => params[:search]}, :class => "outlet"%></li>
  <% elsif action_name == 'outlet_search' %>
      <li><%= link_to "", {:controller => 'home', :action => 'map_search',:location => params[:location],  :longitude => params[:longitude], :latitude => params[:latitude], :search => params[:search]}, :class => "loc"%></li>
  <% end %>
  </ul>

  <nav id="menu">
      <ul>
          <li class="subcat">Sort by:</li>
  <% if action_name == 'map_listing' %>
          <li><%= link_to "Distance", {:controller => 'home', :action => 'map_listing', :location => params[:location], :latitude => params[:latitude], :longitude => params[:longitude]} %></li>
  <% elsif action_name == 'outlet_listing' %>
          <li><%= link_to "Distance", {:controller => 'home', :action => 'outlet_listing', :location => params[:location], :latitude => params[:latitude], :longitude => params[:longitude]} %></li>
  <% elsif action_name == 'map_search' %>
      <li><%= link_to "Distance", {:controller => 'home', :action => 'map_search', :location => params[:location], :latitude => params[:latitude], :longitude => params[:longitude], :search => params[:search]} %></li>
  <% elsif action_name == 'outlet_search' %>
      <li><%= link_to "Distance", {:controller => 'home', :action => 'outlet_search', :location => params[:location], :latitude => params[:latitude], :longitude => params[:longitude], :search => params[:search]} %></li>
  <% end %>
          <li>&nbsp;</li>
          <li class="subcat">Filter by:</li>
          <li style="margin-top:10px;">
          <% location_search_url = get_location_search_url%>
          <%= form_tag location_search_url, :method => 'GET', :id => 'searchForm' do |f|  %>
              <fieldset>
              <div class="input">
                  <%= hidden_field_tag :location, params[:location] %>
                  <%= hidden_field_tag :latitude, params[:latitude] %>
                  <%= hidden_field_tag :longitude, params[:longitude] %>
                  <%= text_field_tag :search, nil,:id => 's', :placeholder => "Enter Category..." %>
              </div>
          </fieldset>
      <% end %>
          </li>
      </ul>
  </nav>
</div>
<%= bootstrap_flash %>
<%= render :partial => "back_to_top" %>
<%= render :partial => "new_mobile_number_modal" %>
<%= render :partial => "verify_mobile_number" %>
<%= render :partial => "share_partial" %>

<%= yield %>
<script type="text/javascript">
$(document).ready(function(){
        $('nav#menu').mmenu();
        modal_submit();
        verification_modal_submit();
        get_backtotop_button();
        })
</script>
</body>
</html>

