<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>GullakMaster - Outletview</title>
<%= csrf_meta_tags %>

<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->

<%= stylesheet_link_tag "application", :media => "all" %>
<%= javascript_include_tag "application" %>

<!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
<!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
<!-- jQuery Plugin -->
<%#= javascript_include_tag :defaults %>


</head>
<body>
<!-- Preloader -->
<div id="header">
  <!--<div class="map-btn-div"><a href="#" class="map-btn-lft2">&nbsp;</a></div>-->
  <ul class="left-icons">
    <li><a href="#menu" class="srchlink3"></a></li>
  </ul>
  <div class="head-brand">GullakMaster</div>
  <ul class="right-icons">
  <% if action_name == 'map_listing' %>
      <li><%= link_to "", {:controller => 'home', :action => 'outlet_listing', :longitude => params[:longitude], :latitude => params[:latitude]}, :class => "outlet"%></li>
  <% else %>
      <li><%= link_to "", {:controller => 'home', :action => 'map_listing', :location => params[:location], :longitude => params[:longitude], :latitude => params[:latitude]}, :class => "loc"%></li>
  <% end %>
  </ul>

  <nav id="menu">
      <ul>
          <li class="subcat">Sort by:</li>
          <li><%= link_to "Distance", {:controller => 'home', :action => 'outlet_listing', :location => params[:location], :latitude => params[:latitude], :longitude => params[:longitude]} %></li>
          <li><%= link_to "Hot Picks", {:controller => 'home', :action => 'hot_picks', :location => params[:location], :latitude => params[:latitude], :longitude => params[:longitude]} %></li>
          <li>&nbsp;</li>
          <li class="subcat">Filter by:</li>
          <li style="margin-top:10px;">
          <%= form_tag :outlet_search, :id => 'searchForm' do |f|  %>
              <fieldset>
              <div class="input">
                  <%= text_field_tag :search, nil,:id => 's', :value => "Enter Category...", :onFocus =>"this.value=''" %>
              </div>
          </fieldset>
      <% end %>
          </li>
      </ul>
  </nav>
</div>
<%= bootstrap_flash %>
<%= yield %>
  <script type="text/javascript">
$(function() {
        $('nav#menu').mmenu();
        });

$(document).ready(function(){$('.alert').fadeOut(2000)});
  </script>

</body>
</html>

