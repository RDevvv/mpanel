<%= form_for(@nested_comment) do |f| %>
  <% if @nested_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nested_comment.errors.count, "error") %> prohibited this nested_comment from being saved:</h2>

      <ul>
      <% @nested_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_id %><br />
    <%= f.text_field :parent_id %>
  </div>
  <div class="field">
    <%= f.label :comment_id %><br />
    <%= f.text_field :comment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
